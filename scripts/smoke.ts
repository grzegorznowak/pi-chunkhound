/**
 * Legacy mechanics suite — ALL checks have been migrated to scripts/tests
 * (phase B4 extraction). This shell remains only so the acceptance adapter
 * (acceptance/legacy-smoke.test.ts) has something to invoke between the last
 * extraction and its own retirement; no assertions live here anymore.
 */
console.log("legacy smoke: all checks migrated to scripts/tests");
